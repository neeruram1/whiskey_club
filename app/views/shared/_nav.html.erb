<%# locals: nav: (hash from nav_config) %>

<nav class="mb-8 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
  <div class="min-w-0">
    <p class="label-xs text-lagavulin-gold/70">
      <%= nav[:label] %>
    </p>

    <% if nav[:title].present? %>
      <h1 class="mt-2 heading-page text-lagavulin-gold">
        <%= nav[:title] %>
      </h1>
    <% end %>
  </div>

  <div class="flex flex-wrap items-center gap-x-6 gap-y-2">
    <% if nav[:back_to].present? %>
      <%= link_to(nav[:back_to], class: "link-mini inline-flex items-center gap-1.5") do %>
        <svg class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"/>
        </svg>
        <span><%= nav[:back_label].presence || "Back" %></span>
      <% end %>
    <% end %>

    <% nav[:links].each do |(text, path)| %>
      <%= link_to text, path, class: "link-mini" %>
    <% end %>

    <% if nav[:primary].present? %>
      <% if nav[:primary].is_a?(Array) && nav[:primary].length == 3 %>
        <% text, path, options = nav[:primary] %>
        <%= link_to text, path, ({ class: "btn-mini" }.merge(options || {})) %>
      <% else %>
        <% text, path = nav[:primary] %>
        <%= link_to text, path, class: "btn-mini" %>
      <% end %>
    <% end %>
  </div>
</nav>
